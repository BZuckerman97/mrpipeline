% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mr.R
\name{run_mr}
\alias{run_mr}
\title{Perform Mendelian randomisation analysis}
\usage{
run_mr(
  exposure,
  exposure_id,
  outcome,
  outcome_id,
  instrument_region = NULL,
  window = 100000L,
  pval_thresh = 5e-08,
  rsq_thresh = 0.001,
  bfile = NULL,
  plink_bin = NULL,
  pop = "EUR",
  instruments = NULL,
  instruments_strict = FALSE,
  exclude_regions = NULL,
  methods = c("ivw_random", "egger", "weighted_median", "presso", "conmix", "steiger"),
  ld_correct = FALSE,
  exposure_n = NULL,
  presso_n_dist = 1000,
  plink_threads = plink_option("threads"),
  plink_memory = plink_option("memory"),
  allele_check = c("error", "warn", "none"),
  harmonise_action = 2,
  verbose = TRUE
)
}
\arguments{
\item{exposure}{Data frame of formatted exposure data (output of
\code{\link[TwoSampleMR:format_data]{TwoSampleMR::format_data()}} or \verb{format_pqtl_*()} functions).}

\item{exposure_id}{Character. Identifier for the exposure (e.g. protein
name).}

\item{outcome}{Data frame of outcome summary statistics with standardised
columns: \code{rsids}, \code{chr}, \code{pos}, \code{beta}, \code{se}, \code{eaf}, \code{pval}, \code{n},
\code{effect_allele}, \code{other_allele}. Formatted internally via
\code{\link[TwoSampleMR:format_data]{TwoSampleMR::format_data()}}.}

\item{outcome_id}{Character. Identifier for the outcome (e.g. disease name).}

\item{instrument_region}{List with elements \code{chromosome}, \code{start}, \code{end}
defining the cis region. \code{NULL} for genome-wide or manual mode.}

\item{window}{Integer. Window (in bp) to extend either side of
\code{instrument_region}. Default \code{100000L}.}

\item{pval_thresh}{Numeric. P-value threshold for instrument selection.
Default \code{5e-8}.}

\item{rsq_thresh}{Numeric. R-squared clumping threshold. Default \code{0.001}.}

\item{bfile}{Character. Path to PLINK bfile prefix for local LD operations.
Required when \code{ld_correct = TRUE}.}

\item{plink_bin}{Character. Path to PLINK binary. Auto-detected if \code{NULL}.}

\item{pop}{Character. Population for API-based LD clumping. Default \code{"EUR"}.}

\item{instruments}{Character vector of rsIDs for manual instrument mode, or
\code{NULL}.}

\item{instruments_strict}{Logical. If \code{TRUE}, error when manual instruments
are missing from exposure data. If \code{FALSE}, warn. Default \code{FALSE}.}

\item{exclude_regions}{Data frame with columns \code{chr}, \code{start}, \code{end} defining
genomic regions to exclude instruments from, or \code{NULL}. For example, to
exclude the MHC region: \code{data.frame(chr = "6", start = 26e6, end = 34e6)}.}

\item{methods}{Character vector of methods to run. Named shortcuts (see
\code{\link[=mr_methods]{mr_methods()}} and the \emph{Available methods} section): \code{"ivw_random"},
\code{"ivw_fixed"}, \code{"egger"}, \code{"weighted_median"}, \code{"presso"}, \code{"conmix"},
\code{"steiger"}, \code{"pleiotropy"}, \code{"heterogeneity"}, \code{"loo"}. You may also
pass any raw method name from \code{TwoSampleMR::mr_method_list()$obj} that
has no shortcut (e.g. \code{"mr_raps"}, \code{"mr_weighted_mode"}); raw names are
never LD-corrected. The former shortcuts \code{"ivw"} and \code{"ivw_fe"} are
accepted with a warning and mapped to \code{"ivw_random"} and \code{"ivw_fixed"}.}

\item{ld_correct}{Logical. Fit \code{ivw_random}, \code{ivw_fixed} and \code{egger} by
GLS with the instruments' LD matrix (see the \emph{LD correction} section).
Requires \code{bfile}. Default \code{FALSE}.}

\item{exposure_n}{Numeric. Exposure sample size. If \code{NULL}, inferred from
\code{samplesize.exposure} column.}

\item{presso_n_dist}{Integer. Number of distributions for MR-PRESSO. Default
\code{1000}.}

\item{plink_threads}{Integer. Number of threads for PLINK. \code{NULL} (default)
lets PLINK auto-detect. Read from \code{getOption("mrpipeline.plink_threads")}
or the \code{MRPIPELINE_PLINK_THREADS} environment variable via
\code{\link[=plink_option]{plink_option()}}.}

\item{plink_memory}{Integer. Memory limit in MB for PLINK. \code{NULL} (default)
lets PLINK auto-detect. Read from \code{getOption("mrpipeline.plink_memory")}
or the \code{MRPIPELINE_PLINK_MEMORY} environment variable via
\code{\link[=plink_option]{plink_option()}}.}

\item{allele_check}{Character. What to do when the allele orientation
check finds that effect/other alleles look swapped between exposure and
outcome -- the signature of a GWAS file whose \code{A1}/\code{A2} mean REF/ALT,
which silently inverts every beta (see \code{\link[=format_gwas]{format_gwas()}}, section \emph{What
does A1 mean?}). \code{"error"} (default) aborts, \code{"warn"} warns and
continues, \code{"none"} runs the analysis regardless. The check harmonises
the instruments together with up to 1000 further SNPs shared by the two
datasets, so it works even for a cis-MR with a handful of instruments;
it is skipped when fewer than 10 informative non-palindromic SNPs carry
both allele frequencies. The full record is available afterwards from
\code{\link[=last_allele_check]{last_allele_check()}} in every mode. The check runs on every call over
the full exposure and outcome you pass in (it is not cached across
calls that share a pair) and costs on the order of seconds when both
are genome-wide -- about 6 s for a 42M-row exposure against a 12M-row
outcome -- recorded in \verb{$timing[["allele_check"]]} so pipeline authors
can plan for it.}

\item{harmonise_action}{\code{1}, \code{2} (default) or \code{3}, passed to
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}}. \code{1} assumes every allele is on the
forward strand; \code{2} infers the positive strand, resolving palindromic
variants from their allele frequencies; \code{3} additionally drops every
palindromic, ambiguous or incompatible SNP. Only palindrome handling
differs -- non-palindromic variants are aligned by allele letter at all
three levels. Reach for \code{3} when the frequencies that level \code{2} relies
on cannot be trusted: a failed allele orientation check makes every
palindromic strand call in that pair unreliable, and a dataset without
allele frequencies gives level \code{2} nothing to resolve them with.}

\item{verbose}{Logical. If \code{TRUE}, emit informational messages via
\code{\link[cli:cli_inform]{cli::cli_inform()}}. Warnings and errors are always emitted regardless.
Default \code{TRUE}.}
}
\value{
An \code{mr_result} object. Check \code{result$status} for \code{"success"} vs
failure reasons. Each \verb{$results} row states the estimator that produced
it: \code{method} (the label), \code{model} (\code{"random"}, \code{"fixed"}, or \code{NA} where
the distinction does not apply) and \code{ld_corrected} (whether the LD
matrix was used for that fit), alongside \code{nsnp}, \code{b}, \code{se}, \code{pval} and
the \code{or}, \code{or_lci95}, \code{or_uci95} columns from
\code{\link[TwoSampleMR:generate_odds_ratios]{TwoSampleMR::generate_odds_ratios()}}. The \verb{$timing} field contains a
named numeric vector of elapsed seconds for each major step.
}
\description{
Runs MR with automatic instrument selection (cis-MR, genome-wide, or manual)
and optional sensitivity analyses. Returns an \code{mr_result} S3 object.
}
\section{Instrument selection modes}{

Exactly one of three modes is used, determined by the combination of
\code{instruments} and \code{instrument_region}:
\itemize{
\item \strong{Cis-MR} (\code{instrument_region} provided, \code{instruments = NULL}): filters
\code{exposure} to the cis region defined by \code{instrument_region} +/- \code{window},
applies \code{pval_thresh}, then LD-clumps.
\item \strong{Genome-wide} (\code{instrument_region = NULL}, \code{instruments = NULL}): filters
\code{exposure} by \code{pval_thresh} only, then LD-clumps.
\item \strong{Manual} (\code{instruments} provided): uses the supplied rsIDs directly.
\code{instruments_strict} controls whether missing IDs are an error or warning.
}
}

\section{Method dispatch}{

Every method is looked up in the package's method registry -- see
\code{\link[=mr_methods]{mr_methods()}} and the \emph{Available methods} section below for what each
shortcut runs, whether it can be LD-corrected, and how many instruments
it needs. Dispatch depends on the number of instruments after clumping:
\itemize{
\item 1 SNP: Wald ratio only; every multi-SNP method is skipped
\item 2+ SNPs: \code{ivw_random}, \code{ivw_fixed}, \code{conmix}, \code{heterogeneity} and any
raw TwoSampleMR method are attempted; \code{egger}, \code{weighted_median},
\code{presso}, \code{pleiotropy} and \code{loo} require >= 3 SNPs
}

Raw TwoSampleMR methods (\verb{mr_*} names from
\code{TwoSampleMR::mr_method_list()$obj} that have no shortcut, e.g.
\code{"mr_raps"}) are dispatched via \code{TwoSampleMR::mr()} under TwoSampleMR's
own label; errors are caught and reported in \verb{$methods_skipped}. Names
that are the engine behind a shortcut (\code{mr_ivw}, \code{mr_ivw_fe},
\code{mr_egger_regression}, \code{mr_weighted_median}) are refused with a pointer
to the shortcut, so the same estimator cannot enter unlabelled and
uncorrected.

ConMix reports \code{se = NA}: \code{MendelianRandomization::mr_conmix()} returns a
confidence interval that may be asymmetric or multi-modal rather than a
standard error, so the \code{or_lci95}/\code{or_uci95} columns are \code{NA} for it too.

When \code{"egger"} is in \code{methods} and there are >= 3 instruments,
\code{TwoSampleMR::mr_pleiotropy_test()} (the Egger intercept test) is always
run automatically and its result stored in \verb{$pleiotropy}. You do not need
to add \code{"pleiotropy"} to \code{methods} separately. The \code{"pleiotropy"} shortcut
remains available for running the intercept test without Egger.
}

\section{LD correction}{

\code{ld_correct = TRUE} computes a signed LD matrix for the instruments from
\code{bfile} and re-orients it to the exposure's effect alleles. Instruments
absent from the reference panel, or with ambiguous palindromic alleles,
are dropped at that step, so an LD-corrected run can have fewer
instruments than the same call uncorrected.

Methods with a correlated form -- \code{ivw_random}, \code{ivw_fixed} and \code{egger}
-- are then fitted by generalised least squares through
\code{MendelianRandomization} with \code{correl = TRUE}: the weight matrix is
\code{diag(se_y) \%*\% R \%*\% diag(se_y)} in place of \code{diag(se_y^2)}, so two
instruments in LD are no longer counted as two independent looks at the
causal effect. The IVW estimator is pinned explicitly (\code{model = "random"}
for \code{ivw_random}, \code{"fixed"} for \code{ivw_fixed}) so that each shortcut means
the same thing at every instrument count; \code{MendelianRandomization}'s own
default would switch to fixed effects below 4 instruments.

Every other \verb{$results} method has no correlated form and runs on the
uncorrected data: a warning names each such method, and its row carries
\code{ld_corrected = FALSE}. The diagnostics come from the correlated fits
too: \verb{$heterogeneity} holds the generalised Cochran Q for correlated
instruments (\verb{Q = r' O^-1 r} on the GLS residuals, from
\code{mr_ivw()@Heter.Stat} and \code{mr_egger()@Heter.Stat}), \verb{$pleiotropy} the
correlated Egger intercept (\code{mr_egger()@Intercept}), and \verb{$loo} a
per-SNP correlated random-effects refit (a block-inverse update, so it
stays O(n^3)). Each of those frames carries an \code{ld_corrected} column on
both arms. \code{steiger} is the one thing left as-is: the Steiger direction
test compares per-SNP r^2 values and involves no weight matrix.
\code{ld_correct} is never silently ignored -- it is either applied, or
visibly not applied. To compare corrected and uncorrected estimates, call
\code{run_mr()} twice and pass both results to \code{\link[=forest_plot]{forest_plot()}} as a named
list: one \code{mr_result} is always one instrument set under one weight
matrix.

If the GLS weight matrix is near-singular (reciprocal condition number
below \code{1e-10}) \code{run_mr()} warns that every LD-corrected estimate is
unstable. The usual causes are identical or near-identical instruments
(r^2 ~ 1, from absent clumping or a manual set with a duplicated
variant) and more instruments than reference-panel individuals, which
makes the sample correlation matrix singular; clump more stringently or
drop the duplicate. When such a matrix is also \emph{solved} -- that is, when
at least one LD-correctable method was requested -- no estimate exists to
return, so \code{run_mr()} stops there with \code{status = "singular_ld_matrix"}
and a \code{status_reason} naming the collinear pairs, rather than letting the
upstream \code{solve()} abort the call (issue #36).

Random effects are multiplicative: the standard error is inflated by
\code{max(RSE, 1)}, never deflated, so when the instruments are under-dispersed
the random- and fixed-effect results coincide exactly.
}

\section{Available methods}{

Rendered from \code{\link[=mr_methods]{mr_methods()}}. \code{label} is the \code{method} value written to
\verb{$results}; \code{model} the effects model; \code{LD} whether \code{ld_correct = TRUE}
applies; \verb{min n} the minimum number of instruments.\tabular{lllllll}{
   shortcut \tab description \tab label \tab output \tab model \tab LD \tab min n \cr
   \code{ivw_random} \tab IVW, multiplicative random effects \tab IVW (random effects) \tab \verb{$results} \tab random \tab yes \tab 2 \cr
   \code{ivw_fixed} \tab IVW, fixed effects \tab IVW (fixed effects) \tab \verb{$results} \tab fixed \tab yes \tab 2 \cr
   \code{egger} \tab MR Egger regression \tab MR Egger \tab \verb{$results} \tab random \tab yes \tab 3 \cr
   \code{weighted_median} \tab Weighted median \tab Weighted median \tab \verb{$results} \tab -- \tab no \tab 3 \cr
   \code{presso} \tab MR-PRESSO outlier test \tab MR-PRESSO \tab \verb{$results} \tab -- \tab no \tab 3 \cr
   \code{conmix} \tab Contamination mixture \tab ConMix \tab \verb{$results} \tab -- \tab no \tab 2 \cr
   \code{steiger} \tab Steiger directionality test \tab -- \tab \verb{$steiger} \tab -- \tab no \tab 1 \cr
   \code{pleiotropy} \tab Egger intercept (pleiotropy) test \tab -- \tab \verb{$pleiotropy} \tab -- \tab yes \tab 3 \cr
   \code{heterogeneity} \tab Cochran's Q heterogeneity test \tab -- \tab \verb{$heterogeneity} \tab -- \tab yes \tab 2 \cr
   \code{loo} \tab Leave-one-out IVW \tab -- \tab \verb{$loo} \tab -- \tab yes \tab 3 \cr
}
}

\examples{
\dontrun{
# Cis-MR using bundled CD40/Sjogren's data
bfile <- sub("\\\\.bed$", "", system.file("extdata", "ld_ref.bed", package = "mrpipeline"))
result <- run_mr(
  exposure = cd40_exposure,
  exposure_id = "CD40",
  outcome = sjogren_outcome,
  outcome_id = "SjD",
  instrument_region = list(chromosome = "20", start = 44746911, end = 44758502),
  rsq_thresh = 0.3,
  bfile = bfile,
  methods = c("ivw_random", "egger", "weighted_median")
)
result
summary(result)

# LD-corrected: both IVW estimators by GLS; weighted median warns and
# runs uncorrected. Compare arms by passing both results to forest_plot().
corrected <- run_mr(
  exposure = cd40_exposure,
  exposure_id = "CD40",
  outcome = sjogren_outcome,
  outcome_id = "SjD",
  instrument_region = list(chromosome = "20", start = 44746911, end = 44758502),
  rsq_thresh = 0.3,
  bfile = bfile,
  ld_correct = TRUE,
  methods = c("ivw_random", "ivw_fixed", "egger", "weighted_median")
)
corrected$results[, c("method", "model", "ld_corrected", "b", "se")]
forest_plot(list("Uncorrected" = result, "LD-corrected" = corrected))
}

}
\seealso{
\code{\link[=mr_methods]{mr_methods()}} for the table of methods and what each supports.
}
