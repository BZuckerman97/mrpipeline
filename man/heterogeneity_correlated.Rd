% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{heterogeneity_correlated}
\alias{heterogeneity_correlated}
\title{Cochran's Q from the correlated fits}
\usage{
heterogeneity_correlated(ld_input, harmonised)
}
\arguments{
\item{ld_input}{An \code{MRInput} carrying the correlation matrix.}

\item{harmonised}{Harmonised instrument data frame (for the id columns).}
}
\value{
A data frame with columns \code{id.exposure}, \code{id.outcome}, \code{outcome},
\code{exposure}, \code{method}, \code{Q}, \code{Q_df}, \code{Q_pval}, \code{ld_corrected}.
}
\description{
The generalised heterogeneity statistic for correlated instruments,
\verb{Q = r' O^-1 r} on the GLS residuals, as \code{mr_ivw(correl = TRUE)@Heter.Stat}
(\code{n - 1} df; identical for fixed and random effects, since Q depends only
on the estimate and the weight matrix) and \code{mr_egger(correl = TRUE)@Heter.Stat}
(\code{n - 2} df, needs \code{n >= 3}). Same columns and row order as
\code{\link[TwoSampleMR:mr_heterogeneity]{TwoSampleMR::mr_heterogeneity()}} (Egger first), plus \code{ld_corrected};
the \code{method} labels are TwoSampleMR's so the column is the only thing
that differs between the two arms.
}
\keyword{internal}
