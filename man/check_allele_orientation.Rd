% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{check_allele_orientation}
\alias{check_allele_orientation}
\title{Detect swapped effect/other alleles from harmonised allele frequencies}
\usage{
check_allele_orientation(
  harmonised,
  allele_check = c("error", "warn", "none"),
  threshold = 0.7,
  min_n = 10L,
  n_sampled = NA_integer_,
  verbose = FALSE,
  call = rlang::caller_env()
)
}
\arguments{
\item{harmonised}{Raw output of \code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} (before
any \code{mr_keep} filtering). Needs columns \code{SNP}, \code{eaf.exposure},
\code{eaf.outcome}, \code{effect_allele.exposure}, \code{other_allele.exposure},
\code{palindromic} and \code{remove}; otherwise the check is skipped.}

\item{allele_check}{One of \code{"error"} (default), \code{"warn"} or \code{"none"}.
Controls what happens on failure; the diagnostic record is stored in
every mode. \code{"none"} also suppresses the warning emitted when the check
cannot reach a verdict.}

\item{threshold}{Proportion of informative variants that must be
complementary for the check to fail. Default \code{0.70}.}

\item{min_n}{Minimum number of informative variants required to reach a
verdict. Default \code{10L}.}

\item{n_sampled}{Integer. Number of non-instrument SNPs that
\code{\link[=check_allele_orientation_gwas]{check_allele_orientation_gwas()}} added to the harmonised set, for the
record only. \code{NA} (default) when the check ran on a harmonisation that
was not sampled.}

\item{verbose}{Logical. If \code{TRUE}, report a passing verdict via
\code{\link[cli:cli_inform]{cli::cli_inform()}}. A verdict that could \emph{not} be reached is always
reported, as a warning, regardless of \code{verbose} -- unless
\code{allele_check = "none"}, which silences it along with the check itself.
Default \code{FALSE}.}

\item{call}{Environment. The calling frame reported in the condition.
Default \code{\link[rlang:caller_env]{rlang::caller_env()}}.}
}
\value{
The diagnostic record (see \code{\link[=last_allele_check]{last_allele_check()}} for its
structure), invisibly. The same record is stored so that
\code{\link[=last_allele_check]{last_allele_check()}} can return it -- this matters on the error path,
where the return value is otherwise lost.
}
\description{
Some GWAS files label their allele columns \code{A1}/\code{A2} meaning REF/ALT, with
\code{BETA} and the frequency column oriented to \code{A2} (EPACTS/RAREMETAL style),
whereas \code{\link[=format_gwas]{format_gwas()}} -- like PLINK, regenie and METAL -- reads \code{A1} as
the effect allele. Feeding such a file in swaps effect and other allele
for every variant and silently inverts every beta (GitHub issue #18). This
check detects that at harmonisation time, the only point where two
datasets coexist, so no external reference panel is needed.
}
\section{How the verdict is reached}{

\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} aligns non-palindromic variants purely by
allele letters, negating \code{beta.outcome} and replacing \code{eaf.outcome} with
\code{1 - eaf.outcome} whenever the outcome's effect allele is the exposure's
other allele. When one dataset's allele labels are swapped, that alignment
is applied to \emph{every} variant, so after harmonisation \code{eaf.outcome} ends
up describing the other allele: \code{eaf.outcome ~ 1 - eaf.exposure} across
the set. Genuine cohort differences (e.g. ancestry) produce \emph{scatter}
around \code{eaf.exposure}; this bug produces systematic \emph{complementarity}.

The statistic is therefore: among \emph{informative} variants -- both EAFs
present, \code{palindromic == FALSE}, \code{remove == FALSE}, one row per SNP --
the proportion whose \code{eaf.exposure} is closer to \code{1 - eaf.outcome} than
to \code{eaf.outcome} (ties, e.g. \code{eaf.outcome == 0.5}, count as \emph{not}
complementary). The check fails when that proportion exceeds \code{threshold}
and at least \code{min_n} informative variants were available; with fewer it
is \code{"skipped"} -- and a warning says so, because a pair that could not be
checked is not a pair that passed, and the two must not look alike in a
log (GitHub issue #21). Variants with EAF near 0.5 are equally likely to fall
either side, so they can only dilute the proportion towards 0.5 -- they
cannot cause a spurious failure, only mask a real one, which the 0.70
threshold tolerates.
}

\section{Why palindromic variants are excluded}{

For A/T and C/G variants the strand cannot be resolved from letters, so
\code{harmonise_data()} resolves it \emph{from the allele frequencies}: after the
letter-based swap it flips again if \code{eaf.exposure} and \code{eaf.outcome} sit
on opposite sides of 0.5. When the effect allele and the frequency are
both mis-assigned those two flips cancel, so a palindromic variant's
\code{eaf.outcome} always looks consistent and its harmonised beta can be
\emph{identical} to the correct value even though every non-palindromic beta
in the same set is inverted. Including them would only dilute the
statistic; and once the check fails, every palindromic strand call in that
pair is unreliable regardless of what its beta looks like.
}

\section{What the check cannot tell you}{

The comparison is symmetric: a failure means the two datasets disagree,
not which one is wrong. Break the tie with an independent frequency
reference (\code{plink --freq} on the LD panel; see \code{ref_frq} in
\code{\link[=format_gwas]{format_gwas()}}), a variant with a well-established effect direction, or
provenance (a dataset that has harmonised cleanly against others is not
the suspect).
}

\keyword{internal}
