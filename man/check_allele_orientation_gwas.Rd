% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{check_allele_orientation_gwas}
\alias{check_allele_orientation_gwas}
\title{Allele orientation check on instruments plus a sample of shared SNPs}
\usage{
check_allele_orientation_gwas(
  exposure,
  outcome,
  instrument_snps,
  allele_check = c("error", "warn", "none"),
  n_sample = 1000L,
  action = 2,
  verbose = FALSE,
  call = rlang::caller_env()
)
}
\arguments{
\item{exposure}{Data frame of TwoSampleMR-formatted exposure data (the
full dataset passed to \code{run_mr()}, not just the instruments).}

\item{outcome}{Data frame in \code{\link[=format_gwas]{format_gwas()}} outcome format (\code{rsids},
\code{effect_allele}, \code{other_allele}, \code{beta}, \code{se}, \code{eaf}, ...).}

\item{instrument_snps}{Character vector of instrument rsIDs to always
include in the check set.}

\item{allele_check}{One of \code{"error"} (default), \code{"warn"} or \code{"none"}.}

\item{n_sample}{Integer. Maximum number of non-instrument shared SNPs to
add. Default \code{1000L}.}

\item{action}{\code{1}, \code{2} (default) or \code{3}. Passed to
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}}, so that the check describes the same
harmonisation the analysis will use. The verdict itself is unaffected:
\code{action} gates only the frequency-based second flip applied to
palindromic variants, which the check excludes anyway.}

\item{verbose}{Logical. Passed to \code{\link[=check_allele_orientation]{check_allele_orientation()}}. A check
that cannot reach a verdict warns regardless.}

\item{call}{Environment reported in the condition. Default
\code{\link[rlang:caller_env]{rlang::caller_env()}}.}
}
\value{
The diagnostic record, invisibly (see \code{\link[=last_allele_check]{last_allele_check()}}).
}
\description{
\code{\link[=run_mr]{run_mr()}}'s instrument set is often too small for
\code{\link[=check_allele_orientation]{check_allele_orientation()}} to reach a verdict (a cis-MR may have three
instruments; the check needs ten informative non-palindromic SNPs). But
\code{run_mr()} holds the \emph{full} exposure and outcome GWAS before it narrows
the outcome to instrument rsIDs, so this helper harmonises the
instruments together with up to \code{n_sample} additional SNPs shared by the
two datasets and runs the verdict on that larger set. The extra SNPs are
taken at evenly spaced positions through the sorted shared rsIDs --
deterministic, so results are reproducible and the caller's RNG state is
untouched, and effectively random with respect to genomic position.
}
\details{
Cost scales with the total rows of the two inputs, not with the number
of instruments: the rsID intersection and the radix sort of the shared
rsIDs are the only steps that touch every row (about 6 s for a 42M-row
exposure against a 12M-row outcome, i.e. two genome-wide GWAS; well under
a second for a 200k-SNP exposure). Formatting and harmonising the ~1000
sampled SNPs takes a few milliseconds. The shared rsIDs are sorted with
\code{method = "radix"}: base \code{sort()} uses shell sort with per-comparison
locale collation for character vectors, which is ~25x slower on 10M
rsIDs and made the check the dominant cost of a cis-MR (issue #40). The
check needs the \emph{full} frames to sample from, so it cannot be made
cheaper by narrowing the inputs first, and it runs on every call -- it
is not cached across \code{run_mr()} calls that share a pair.

The check is skipped (with a \code{"skipped"} record, and a warning that
orientation is unverified) when the exposure lacks \code{SNP}/\code{eaf.exposure} or
the outcome lacks the \code{\link[=format_gwas]{format_gwas()}} outcome columns, or when nothing
overlaps.
}
\keyword{internal}
