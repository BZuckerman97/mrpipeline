% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_single_cell_eqtl.R
\name{format_sceqtl_dice}
\alias{format_sceqtl_dice}
\title{Format single-cell RNA eQTL data from DICE for MR analysis}
\usage{
format_sceqtl_dice(file, cell_type = NULL)
}
\arguments{
\item{file}{Character. Path to a DICE eQTL VCF file
(e.g., \code{"t_cell_cd4_naive.vcf"}). Plain and gzip-compressed files are
both supported.}

\item{cell_type}{Character or \code{NULL}. Cell type label used in the phenotype
string (\code{GENE___cell_type}). If \code{NULL}, derived from the filename by
stripping the \code{.vcf} (or \code{.vcf.gz}) extension.}
}
\value{
A data frame formatted as a TwoSampleMR exposure dataset, including
\code{chr.exposure} and \code{pos.exposure} columns required by \code{\link[=run_coloc]{run_coloc()}}.
Note: EAF is not available in DICE VCF files; \code{eaf.exposure} will be
absent and \code{exposure_n} must be supplied explicitly to \code{\link[=run_coloc]{run_coloc()}}.
}
\description{
Reads and formats eQTL summary statistics from the Database of Immune Cell
Expression, eQTLs and Epigenomics (DICE; Schmiedel et al. 2018) for a
single cell-type VCF file.
}
\details{
The VCF INFO field is parsed with regex to extract \code{Gene}, \code{GeneSymbol},
\code{Pvalue}, and \code{Beta}. SE is derived as
\verb{se = |Beta| / sqrt(qchisq(Pvalue, df = 1, lower.tail = FALSE))}. Only
biallelic SNPs (single-nucleotide, A/C/G/T) are retained. Palindromic SNPs
(A/T and C/G pairs) are dropped because DICE provides no allele frequency
data to resolve strand ambiguity. \code{ALT} is treated as the effect allele and
\code{REF} as the other allele. The exposure phenotype label is
\verb{GeneSymbol___<cell_type>}, falling back to \code{Gene} when \code{GeneSymbol} is
absent.
}
\examples{
\dontrun{
exposure <- format_sceqtl_dice(file = "t_cell_cd4_naive.vcf")
}
}
\seealso{
\code{\link[=run_mr]{run_mr()}}, \code{\link[=run_coloc]{run_coloc()}}

Other sceqtl-format:
\code{\link[=format_sceqtl_1m_scbloodnl]{format_sceqtl_1m_scbloodnl()}},
\code{\link[=format_sceqtl_dynamic_cseqtl]{format_sceqtl_dynamic_cseqtl()}},
\code{\link[=format_single_cell_onek1k]{format_single_cell_onek1k()}}
}
\concept{sceqtl-format}
