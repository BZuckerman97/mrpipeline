% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.mr_result}
\alias{plot.mr_result}
\title{Plot an mr_result object}
\usage{
\method{plot}{mr_result}(x, type = c("scatter", "forest", "funnel", "loo"), ...)
}
\arguments{
\item{x}{An \code{mr_result} object.}

\item{type}{Character. Plot type: \code{"scatter"} (default), \code{"forest"},
\code{"funnel"}, or \code{"loo"} (leave-one-out, via
\code{\link[TwoSampleMR:mr_leaveoneout_plot]{TwoSampleMR::mr_leaveoneout_plot()}} on \code{x$loo}; requires \code{"loo"} to
have been in \code{methods} when \code{run_mr()} was called).}

\item{...}{Ignored.}
}
\value{
A ggplot object (or list of ggplot objects for \code{"scatter"} and
\code{"loo"}). Returns \code{NULL} invisibly if the result status is not
\code{"success"}, if no results are available, or (for \code{type = "loo"}) if
\code{x$loo} was not computed.
}
\description{
Creates diagnostic plots for Mendelian randomisation results using
TwoSampleMR plotting functions. Requires the \code{ggplot2} package.
}
\examples{
\dontrun{
bfile <- sub("\\\\.bed$", "", system.file("extdata", "ld_ref.bed", package = "mrpipeline"))
result <- run_mr(
  exposure = cd40_exposure, exposure_id = "CD40",
  outcome = sjogren_outcome, outcome_id = "SjD",
  instrument_region = list(chromosome = "20", start = 44746911, end = 44758502),
  bfile = bfile,
  methods = c("ivw", "egger", "weighted_median", "loo")
)
plot(result, type = "scatter")
plot(result, type = "forest")
plot(result, type = "funnel")
plot(result, type = "loo")
}

}
