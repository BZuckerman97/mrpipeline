% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_coloc_locuszoom}
\alias{plot_coloc_locuszoom}
\title{LD-coloured regional plots for coloc results, via locuszoomr}
\usage{
plot_coloc_locuszoom(x, ens_db, bfile, plink_bin = NULL, index_snp = NULL, ...)
}
\arguments{
\item{x}{A \code{coloc_result} object.}

\item{ens_db}{Either an \code{ensembldb::EnsDb} object, or the name of an
Ensembl annotation package as a string (e.g. \code{"EnsDb.Hsapiens.v75"}),
matching the genome build of \code{x}'s data. If given as a string, the
package must be \strong{attached} first (\code{library(EnsDb.Hsapiens.v75)}), not
merely installed -- \code{locuszoomr::locus()} resolves a character \code{ens_db}
via \code{get(ens_db)} on the search path, and errors
("Ensembl database not loaded") if it isn't attached.}

\item{bfile}{Path to PLINK bfile prefix (without .bed/.bim/.fam), used as
the local LD reference panel.}

\item{plink_bin}{Path to PLINK binary. If \code{NULL}, auto-detected via
\code{\link[genetics.binaRies:get_plink_binary]{genetics.binaRies::get_plink_binary()}}.}

\item{index_snp}{rsID to colour LD against and centre the plotted region
on. Defaults to the SNP with the lowest \code{pval.exposure}, matching
\code{locuszoomr::locus()}'s own default when no index SNP is given.}

\item{...}{Forwarded to \code{\link[=compute_ld_to_index]{compute_ld_to_index()}} (\code{plink_threads},
\code{plink_memory}).}
}
\value{
\code{NULL}, invisibly. Like base/grid plotting functions (and unlike
this package's other \verb{plot_coloc_*()} functions, which return a ggplot
object), this draws directly to the current graphics device as a side
effect -- open a device (\code{pdf()}, \code{png()}, ...) before calling, and
\code{dev.off()} after, to save the result.
}
\description{
Builds exposure and outcome regional plots using \code{locuszoomr::locus()},
coloured by LD (r2) to a chosen index SNP, and stacks them with
\code{locuszoomr::multi_layout()}. LD is computed from a local reference panel
via \code{\link[=compute_ld_to_index]{compute_ld_to_index()}} (reusing \code{\link[=compute_ld_matrix]{compute_ld_matrix()}} -- no network
lookups).
}
\details{
There is no genome-build field stored on a \code{coloc_result} -- \code{ens_db} must
match whatever build the exposure/outcome data (and \code{bfile}) actually use
(e.g. \code{EnsDb.Hsapiens.v75} for GRCh37, \code{EnsDb.Hsapiens.v86} for GRCh38).
}
\keyword{internal}
