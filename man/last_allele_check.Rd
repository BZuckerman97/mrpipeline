% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/last_allele_check.R
\name{last_allele_check}
\alias{last_allele_check}
\title{Retrieve the most recent allele orientation check}
\usage{
last_allele_check()
}
\value{
The record list, or \code{NULL} (with a message) if no check has run
yet in this session.
}
\description{
Every call to \code{\link[=run_mr]{run_mr()}} or \code{\link[=run_coloc]{run_coloc()}} runs an allele orientation check
after harmonisation (see \code{\link[=check_allele_orientation]{check_allele_orientation()}}) and stores its full
diagnostic record, whether it passed, failed or was skipped. This function
returns that record, following the \code{httr2::last_response()} pattern. It is
most useful after an \code{mrpipeline_allele_check_error} -- when the analysis
function's return value is lost -- to inspect every variant behind the
verdict, but the record is stored on a pass too, so a borderline result
can still be examined.
}
\details{
The check detects a GWAS file whose \code{A1}/\code{A2} columns mean REF/ALT rather
than effect/other, which silently inverts every beta. See the \emph{What does
A1 mean?} section of \code{\link[=format_gwas]{format_gwas()}} for the two conventions and how to
tell them apart.
}
\section{Record structure}{

A named list:
\itemize{
\item \code{status}: \code{"pass"}, \code{"fail"} or \code{"skipped"} (fewer than \code{min_n}
informative SNPs, or no allele frequencies).
\item \code{n}: number of informative SNPs -- non-palindromic, alleles compatible,
both \code{eaf.exposure} and \code{eaf.outcome} present.
\item \code{n_complementary}: how many of those have \code{eaf.exposure} closer to
\code{1 - eaf.outcome} than to \code{eaf.outcome}.
\item \code{prop}: \code{n_complementary / n}.
\item \code{threshold}, \code{min_n}: the criteria in force (fail when \code{prop > threshold} and \code{n >= min_n}).
\item \code{allele_check}: the mode in force (\code{"error"}, \code{"warn"} or \code{"none"}).
\item \code{exposure}, \code{outcome}: phenotype names of the harmonised pair (the
\code{exposure_id}/\code{outcome_id} passed to \code{\link[=run_mr]{run_mr()}} or \code{\link[=run_coloc]{run_coloc()}}), plus
\code{id.exposure}, \code{id.outcome}, TwoSampleMR's internal identifiers.
\item \code{n_sampled}: for \code{\link[=run_mr]{run_mr()}}, the number of non-instrument SNPs shared by
the two datasets that were added to the check set (up to 1000); \code{NA}
for \code{\link[=run_coloc]{run_coloc()}}, which checks every SNP in its window.
\item \code{variants}: a data frame with one row per SNP that had both allele
frequencies -- palindromic and allele-incompatible SNPs included but
flagged -- with columns \code{SNP}, \code{effect_allele}, \code{other_allele},
\code{eaf.exposure}, \code{eaf.outcome}, \code{eaf.outcome_flipped} (\code{1 - eaf.outcome}), \code{palindromic}, \code{remove}, \code{informative}, \code{score}
(positive when complementary; larger is more clear-cut) and
\code{complementary} (\code{NA} for non-informative rows). Sort by \code{score}
descending to see the worst offenders.
}
}

\examples{
\dontrun{
result <- run_mr(exposure, "CD40", outcome, "AMD", instruments = ivs)

chk <- last_allele_check()
chk$status
chk$variants[order(-chk$variants$score), ]
}

}
\seealso{
\code{\link[=run_mr]{run_mr()}} and \code{\link[=run_coloc]{run_coloc()}} (\code{allele_check} argument),
\code{\link[=format_gwas]{format_gwas()}} (section \emph{What does A1 mean?}).
}
