% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{collinear_pairs}
\alias{collinear_pairs}
\title{Instrument pairs that are (near-)perfectly correlated}
\usage{
collinear_pairs(ld_matrix, threshold = 0.999)
}
\arguments{
\item{ld_matrix}{Signed, aligned LD correlation matrix.}

\item{threshold}{Absolute correlation at or above which a pair is
reported. Default \code{0.999}.}
}
\value{
A data frame with columns \code{snp_a}, \code{snp_b} and \code{r}, ordered by
decreasing \code{abs(r)}; zero rows when no pair reaches \code{threshold}.
}
\description{
The usual reason a GLS weight matrix is singular: two instruments in
complete LD carry the same column, so \code{R} has a zero eigenvalue and
\code{solve()} fails. Used by \code{run_mr()} to name the offending pairs rather
than report the condition number alone (issue #36).
}
\keyword{internal}
