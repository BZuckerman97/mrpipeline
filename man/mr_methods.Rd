% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_methods.R
\name{mr_methods}
\alias{mr_methods}
\title{Methods available to run_mr()}
\usage{
mr_methods(detail = c("concise", "full"))
}
\arguments{
\item{detail}{\code{"concise"} (default) lists the ten shortcuts that can be
passed to \verb{methods=}. \code{"full"} adds the two paths that have no single
name -- the automatic Wald ratio used at exactly one instrument, and the
raw TwoSampleMR passthrough -- plus the \code{engine} and \code{engine_ld} columns
naming the function that runs on each LD path.}
}
\value{
A data frame with columns:
\describe{
\item{\code{shortcut}}{Name accepted by \code{run_mr(methods = )}. \code{NA} for the
two rows that cannot be requested by a single name.}
\item{\code{description}}{What the method does.}
\item{\code{label}}{The \code{method} value written to \verb{$results}; \code{NA} for
diagnostics, which produce no results row.}
\item{\code{output}}{Where the result lands: \verb{$results}, \verb{$steiger},
\verb{$pleiotropy}, \verb{$heterogeneity} or \verb{$loo}.}
\item{\code{model}}{\code{"random"}, \code{"fixed"}, or \code{NA} where the distinction
does not apply. Random effects are multiplicative: the standard
error is inflated by \code{max(RSE, 1)} and never deflated.}
\item{\code{ld_correctable}}{Whether \code{ld_correct = TRUE} changes this
method. Methods with \code{FALSE} run on uncorrected data and warn.}
\item{\code{min_instruments}}{Minimum number of instruments; below it the
method is skipped and \verb{$methods_skipped} records why.}
\item{\code{engine}, \code{engine_ld}}{(\code{detail = "full"} only) The function
mrpipeline calls when \code{ld_correct} is \code{FALSE} / \code{TRUE}. Documentary
-- users never pass an engine; \code{run_mr()} picks it from the
shortcut and \code{ld_correct}.}
}
}
\description{
Returns the table of methods \code{\link[=run_mr]{run_mr()}} understands: what each shortcut
runs, whether it is a fixed- or random-effects estimator, whether
\code{ld_correct = TRUE} applies to it, how many instruments it needs, and
where its result lands on the returned \code{mr_result}. The table is the same
object \code{run_mr()} dispatches from (see \code{\link[=mr_method_registry]{mr_method_registry()}}), so it is
always current.
}
\examples{
mr_methods()
mr_methods(detail = "full")

}
\seealso{
\code{\link[=run_mr]{run_mr()}}
}
