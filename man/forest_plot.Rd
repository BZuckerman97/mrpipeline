% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{forest_plot}
\alias{forest_plot}
\title{Forest plot of MR methods for one or more results}
\usage{
forest_plot(
  results,
  methods = c("IVW (fixed effects)", "Inverse variance weighted", "MR Egger",
    "Weighted median"),
  relabel = c(`Inverse variance weighted` = "IVW (random effects)"),
  exponentiate = TRUE,
  xlab = NULL,
  trans = NULL,
  xlim = NULL,
  shape_points = 15,
  colour_scheme = "black",
  col_text_size = 5,
  subheading_size = 6,
  col1_width = 1,
  ...
)
}
\arguments{
\item{results}{An \code{mr_result}, or a list of \code{mr_result} objects. If the
list has more than one element, every element must be named -- names
become section labels (e.g. \code{list(Primary = res1, "Positive control" = res2)}). A single result (bare, or a length-1 list) is never sectioned,
regardless of any name given.}

\item{methods}{Character vector of \code{method} values to include, in display
order. Default puts fixed effects above random effects: \code{c("IVW (fixed effects)", "Inverse variance weighted", "MR Egger", "Weighted median")}.}

\item{relabel}{Named character vector, \code{c(old = new)}, applied to the
\code{method} column for display only, after row filtering/ordering -- so
matching against \code{results$results$method} is unaffected. Default
relabels \code{"Inverse variance weighted"} to \code{"IVW (random effects)"}.}

\item{exponentiate}{Logical. Plot on the OR scale. Default \code{TRUE}.}

\item{xlab}{X-axis label. Defaults to \code{"OR (95\% CI)"} if \code{exponentiate},
else \code{"Effect (95\% CI)"}.}

\item{trans}{X-axis scale transform, e.g. \code{"identity"}, \code{"log2"}.
Defaults to \code{"log2"} if \code{exponentiate}, else \code{"identity"}.}

\item{xlim}{Numeric vector \code{c(lower, upper)} giving x-axis limits, or
\code{NULL} for automatic.}

\item{shape_points}{Point shape (see \code{ggplot2::geom_point()}'s \code{shape}).}

\item{colour_scheme}{Text/point colour. Note: font \emph{family} is not
configurable -- \code{TwoSampleMR::forest_plot_1_to_many()} has no such
argument.}

\item{col_text_size}{Row-label font size.}

\item{subheading_size}{Section-heading font size.}

\item{col1_width}{Width of the row-label column.}

\item{...}{Forwarded to \code{TwoSampleMR::forest_plot_1_to_many()} (e.g.
\code{addcols}, \code{addcol_widths}, \code{addcol_titles}, \code{weight}).}
}
\value{
A grid plot object, or \code{NULL} invisibly if no result has
successful, matching rows.
}
\description{
One row per method (e.g. IVW fixed/random effects, MR Egger, weighted
median), for a single \code{mr_result} or several sectioned together. Wraps
\code{\link[TwoSampleMR:forest_plot_1_to_many]{TwoSampleMR::forest_plot_1_to_many()}}. Requires the \code{ggplot2} package.
}
\examples{
\dontrun{
bfile <- sub("\\\\.bed$", "", system.file("extdata", "ld_ref.bed", package = "mrpipeline"))
result <- run_mr(
  exposure = cd40_exposure, exposure_id = "CD40",
  outcome = sjogren_outcome, outcome_id = "SjD",
  instrument_region = list(chromosome = "20", start = 44746911, end = 44758502),
  bfile = bfile,
  methods = c("ivw", "ivw_fe", "egger", "weighted_median")
)

# Single result: one row per method
forest_plot(result)

# Several results sectioned together, e.g. primary analysis plus controls
forest_plot(list(
  Primary = result,
  "Positive control" = positive_control_result,
  "Negative control" = negative_control_result
))
}

}
