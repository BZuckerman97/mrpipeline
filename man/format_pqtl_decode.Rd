% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_pqtl_decode.R
\name{format_pqtl_decode}
\alias{format_pqtl_decode}
\title{Formats deCODE genetics proteomic data for Mendelian Randomization analysis}
\usage{
format_pqtl_decode(
  decode_proteomic_gwas_file_path,
  decode_included_variants_file_path,
  pqtl_assay,
  x_y_chr_file = NULL,
  type = c("exposure", "outcome")
)
}
\arguments{
\item{decode_proteomic_gwas_file_path}{Character vector of file path(s) to deCODE GWAS data,
or a single pre-loaded data.frame. If multiple paths are provided, data will be combined.}

\item{decode_included_variants_file_path}{Character vector of file path(s) to the deCODE
included variants data, or a single pre-loaded data.frame. If multiple paths are provided,
data will be combined.}

\item{pqtl_assay}{String, name of the deCODE genetics protein assayed}

\item{x_y_chr_file}{String, optional file path to a file containing rsids for X and Y
chromosomes. This file should be tab-separated with columns: Chromosome, Position, RSID.}

\item{type}{Character, either \code{"exposure"} (default) or \code{"outcome"}.
\code{"exposure"} runs the data through \code{\link[TwoSampleMR:format_data]{TwoSampleMR::format_data()}} and
returns \verb{list(exposure = <formatted data frame>)}. \code{"outcome"} skips
\code{format_data()} and returns a plain data frame normalised to the same
schema as \code{format_gwas(type = "outcome")}, so \code{\link[=run_mr]{run_mr()}} can pre-filter
by rsids before calling \code{format_data()} internally.}
}
\value{
For \code{type = "exposure"}: a list with one element, \code{exposure}
(formatted exposure data frame, output of TwoSampleMR::format_data()).
For \code{type = "outcome"}: a plain data frame (not wrapped in a list).
}
\description{
This function filters deCODE genetics GWAS by the deCODE included variants file,
processes the data to clean the column headings and uses TwoSampleMR::format_data() to prepare
the exposure dataset before run_mr()
}
\examples{
\dontrun{
result <- format_pqtl_decode(
  decode_proteomic_gwas_file_path = "path/to/decode_gwas.txt.gz",
  decode_included_variants_file_path = "path/to/included_variants.txt.gz",
  pqtl_assay = "CD40"
)
head(result$exposure)
}

}
