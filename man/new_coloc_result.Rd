% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coloc_result.R
\name{new_coloc_result}
\alias{new_coloc_result}
\title{Create a coloc_result object}
\usage{
new_coloc_result(
  exposure_id = NULL,
  outcome_id = NULL,
  coloc_abf = NULL,
  coloc_susie = NULL,
  coloc_signals = NULL,
  coloc_prop_test = NULL,
  n_snps = 0L,
  harmonised_data = data.frame(),
  harmonisation = data.frame(),
  methods_skipped = character(),
  params = list(),
  status = "success",
  status_reason = NULL,
  timing = numeric(0)
)
}
\arguments{
\item{exposure_id}{Character. Label for the exposure trait.}

\item{outcome_id}{Character. Label for the outcome trait.}

\item{coloc_abf}{Output of \code{\link[coloc:coloc.abf]{coloc::coloc.abf()}}, or \code{NULL}.}

\item{coloc_susie}{Output of \code{\link[coloc:coloc.susie]{coloc::coloc.susie()}}, or \code{NULL}.}

\item{coloc_signals}{Output of \code{\link[coloc:coloc.signals]{coloc::coloc.signals()}}, or \code{NULL}.}

\item{coloc_prop_test}{Output of \code{colocPropTest::coloc.prop.test()}, or
\code{NULL}.}

\item{n_snps}{Integer. Number of SNPs used in the analysis.}

\item{harmonised_data}{Data frame of the harmonised data the analysis
actually ran on: filtered, and subset and reordered by
\code{\link[=align_to_ld_matrix]{align_to_ld_matrix()}} to match the LD matrix, so its rows correspond
one-to-one with the coloc datasets. The plotting functions rely on that
correspondence.}

\item{harmonisation}{Data frame. The complete, unfiltered
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} output -- every candidate variant, with
the \code{mr_keep}, \code{palindromic}, \code{ambiguous} and \code{remove} flags that explain
why each one was or was not carried forward. Kept separate from
\code{harmonised_data} rather than replacing it (GitHub issue #17); see
\code{\link[=harmonisation_summary]{harmonisation_summary()}}.}

\item{methods_skipped}{Named character vector: names are method names,
values are reasons for skipping.}

\item{params}{List of all input parameters to \code{run_coloc()}.}

\item{status}{Character. One of \code{"success"}, \code{"no_snps_in_region"},
\code{"too_few_snps"}, \code{"no_harmonised_variants"}. Default \code{"success"}.}

\item{status_reason}{Character or \code{NULL}. Human-readable explanation when
\code{status != "success"}.}

\item{timing}{Named numeric vector of elapsed times (seconds) for each
major step inside \code{run_coloc()}. Empty by default.}
}
\value{
An object of class \code{coloc_result}.
}
\description{
Create a coloc_result object
}
\keyword{internal}
