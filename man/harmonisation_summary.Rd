% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{harmonisation_summary}
\alias{harmonisation_summary}
\title{Summarise what happened during harmonisation}
\usage{
harmonisation_summary(raw)
}
\arguments{
\item{raw}{The \code{raw} element of \code{\link[=harmonise_and_filter]{harmonise_and_filter()}}'s return value.}
}
\value{
A named list of integers: \code{n_candidates}, \code{n_kept}, \code{n_dropped},
\code{n_duplicate}, \code{n_palindromic}, \code{n_ambiguous}, \code{n_incompatible} and
\code{n_incomplete}. All zero when \code{raw} is empty or lacks the flag columns.
}
\description{
Counts, from the unfiltered \code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} output, how
many variants were carried forward and why the rest were not. Used by
\code{\link[=summary.mr_result]{summary.mr_result()}} and \code{\link[=summary.coloc_result]{summary.coloc_result()}}.
}
\details{
A variant can carry more than one flag -- an ambiguous variant is by
definition palindromic -- so the reason counts are \emph{not} a partition of
\code{n_dropped} and must not be presented as one. Which flags actually cost a
variant its place depends on the harmonisation action: \code{remove} at every
level, \code{ambiguous} from level 2, \code{palindromic} only at level 3 (see
\code{\link[=validate_harmonise_action]{validate_harmonise_action()}}). \code{n_incomplete} covers the separate case of
a variant dropped by \code{harmonise_data()} for missing beta/se rather than
for any allele problem.
}
\keyword{internal}
