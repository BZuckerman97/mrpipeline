% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{validate_harmonise_action}
\alias{validate_harmonise_action}
\title{Validate a TwoSampleMR harmonisation action level}
\usage{
validate_harmonise_action(action)
}
\arguments{
\item{action}{Value to validate.}
}
\value{
\code{action}, unchanged. It is deliberately not coerced to integer:
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} embeds \code{action} as a column in its
output, so coercing \code{2} to \code{2L} would make \code{mrpipeline}'s harmonised
frame differ from a plain \code{harmonise_data()} call on the same data by
the storage mode of that column alone.
}
\description{
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} accepts \code{action} as a vector, applying a
different level per outcome. \code{run_mr()} and \code{run_coloc()} handle exactly
one outcome, so a vector here is a mistake worth catching rather than
silently recycling.
}
\details{
The three levels:\tabular{ll}{
   \code{action} \tab Behaviour \cr
   1 \tab Assume all alleles are on the forward strand: no frequency-based flip \cr
   2 \tab Infer the positive strand, resolving palindromes from allele frequencies (default) \cr
   3 \tab As 2, but set \code{mr_keep = FALSE} for every palindromic, ambiguous or incompatible SNP \cr
}


Only the palindrome handling differs. The letter-based alignment of
non-palindromic variants -- negating \code{beta.outcome} and replacing
\code{eaf.outcome} with \code{1 - eaf.outcome} when the outcome's effect allele is
the exposure's other allele -- happens at every level, which is why
\code{\link[=check_allele_orientation]{check_allele_orientation()}}'s verdict does not depend on \code{action}.
}
\keyword{internal}
