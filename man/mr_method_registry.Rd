% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_methods.R
\name{mr_method_registry}
\alias{mr_method_registry}
\title{The MR method registry}
\usage{
mr_method_registry()
}
\value{
A data frame with one row per method path and columns \code{shortcut},
\code{description}, \code{label}, \code{output}, \code{model}, \code{ld_correctable},
\code{min_instruments}, \code{engine}, \code{engine_ld}.
}
\description{
The single source of truth for every method \code{\link[=run_mr]{run_mr()}} can run: its
shortcut name, the label written to \verb{$results$method}, whether it is a
fixed- or random-effects estimator, whether \code{ld_correct = TRUE} applies to
it, the minimum number of instruments it needs, where its output lands on
the \code{mr_result}, and the function that actually runs on each LD path.
\code{run_mr()} reads validation, dispatch, skip reasons and the LD warnings
from this table, and the method tables in \code{?run_mr} and the vignettes are
rendered from it, so the documentation cannot drift from the code. Adding
a method means adding a row here.
}
\details{
Two rows have no \code{shortcut}: the Wald ratio, which \code{run_mr()} uses
automatically when exactly one instrument survives, and the raw
TwoSampleMR passthrough, one row standing for every
\code{TwoSampleMR::mr_method_list()$obj} name that has no shortcut.

\code{ld_correctable} is declared, not inferred: nothing inspects upstream
function signatures at run time. The row is kept honest by
\code{engine_ld} -- a method claims LD support only by naming the function
that provides it, and the tests assert \code{ld_correctable == !is.na(engine_ld)}.
There is deliberately no free-text notes column: notes about upstream
internals go stale silently when a dependency changes, whereas
\code{engine}/\code{engine_ld} name what mrpipeline calls and so stay true as long
as the dispatch does.
}
\keyword{internal}
