% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{fread_file}
\alias{fread_file}
\title{Read a delimited file, checking compression support first}
\usage{
fread_file(path, ...)
}
\arguments{
\item{path}{Character scalar file path.}

\item{...}{Passed to \code{\link[data.table:fread]{data.table::fread()}}.}
}
\value{
The value of \code{\link[data.table:fread]{data.table::fread()}}.
}
\description{
Thin wrapper around \code{\link[data.table:fread]{data.table::fread()}} that fails early, with an
actionable message, when \code{path} is compressed but the \code{R.utils} package
(which \code{fread()} needs to decompress \code{.gz}/\code{.bz2} files) is unavailable.
\code{R.utils} is only a \code{Suggests} of \code{data.table}, so declaring it in
\code{mrpipeline}'s \code{Imports} is what actually guarantees the gzipped inputs
that GWAS summary statistics are routinely distributed as (issue #20).
This guard is a backstop for a broken library rather than an expected
failure mode, and keeps the error at the top of the call stack instead of
deep inside \code{fread()}.
}
\keyword{internal}
