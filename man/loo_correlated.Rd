% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{loo_correlated}
\alias{loo_correlated}
\title{Leave-one-out IVW with correlated instruments}
\usage{
loo_correlated(harmonised, ld_matrix, rcond_min = 1e-10)
}
\arguments{
\item{harmonised}{Harmonised instrument data frame, in the matrix's row
order.}

\item{ld_matrix}{Signed, aligned LD correlation matrix.}

\item{rcond_min}{Reciprocal-condition-number threshold below which direct
per-SNP solves replace the block-inverse update.}
}
\value{
A data frame in the columns of \code{\link[TwoSampleMR:mr_leaveoneout]{TwoSampleMR::mr_leaveoneout()}}
(\code{exposure}, \code{outcome}, \code{id.exposure}, \code{id.outcome}, \code{samplesize},
\code{SNP}, \code{b}, \code{se}, \code{p}) plus \code{ld_corrected}; one row per dropped SNP and
a final \code{"All"} row.
}
\description{
The correlated counterpart of \code{\link[TwoSampleMR:mr_leaveoneout]{TwoSampleMR::mr_leaveoneout()}}: the
random-effects IVW estimate with each instrument dropped in turn, plus the
pooled \code{"All"} row, fitted with the GLS weight matrix. There is no upstream
implementation -- \code{MendelianRandomization::mr_loo()} takes no \code{correl}
argument and never reads the correlation slot.
}
\details{
Not n refits of \code{mr_ivw()}, which would be n solves of an (n-1)x(n-1)
system (O(n^4) overall: 15 s at n = 300, minutes at n = 600). The full
inverse \code{W = O^-1} is computed once and the inverse with SNP \code{i} removed
is obtained by the Schur-complement identity
\verb{W[-i, -i] - W[-i, i] W[i, -i] / W[i, i]} -- O(n^2) per SNP, O(n^3) in
total, exact (agrees with refits to machine precision). With that in hand
the estimate is exactly what \code{mr_ivw(correl = TRUE, model = "random")}
computes: \verb{b = (bx' W bx)^-1 bx' W by}, residual SE
\verb{sqrt(r' W r / (m - 1))} with \code{m} instruments in the fit, standard error
\verb{sqrt(1 / (bx' W bx)) * max(rse, 1)}, normal p-value. \code{model = "random"}
matches \code{mr_leaveoneout()}'s default, so the two arms differ only in the
weight matrix.

The update divides by \code{W[i, i]}, so on a near-singular \code{O} it inherits the
full inverse's rounding error; below \code{rcond_min} the function falls back to
a direct solve per SNP instead. (An \code{O} that bad already makes the headline
\code{mr_ivw()} fit unstable; \code{run_mr()} warns about it at the source.)
}
\keyword{internal}
