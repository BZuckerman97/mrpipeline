% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_methods.R
\name{mr_result_row}
\alias{mr_result_row}
\title{Build one \verb{$results} row from a registry entry}
\usage{
mr_result_row(
  entry,
  exposure_id,
  outcome_id,
  nsnp,
  b,
  se,
  pval,
  ld_corrected,
  label = entry$label
)
}
\arguments{
\item{entry}{One-row data frame from \code{\link[=mr_method_entry]{mr_method_entry()}}.}

\item{exposure_id, outcome_id}{Labels for the \code{exposure}/\code{outcome} columns.}

\item{nsnp, b, se, pval}{The estimate.}

\item{ld_corrected}{Logical. Whether the LD matrix was used for this fit.}

\item{label}{The \code{method} label. Defaults to the registry label; the raw
TwoSampleMR passthrough supplies TwoSampleMR's own.}
}
\value{
A one-row data frame.
}
\description{
Every \verb{$results} row \code{run_mr()} produces goes through here, so the schema
-- including the \code{ld_corrected} and \code{model} columns that say which
estimator ran -- cannot differ between dispatch branches (which would make
the final \code{rbind} fail).
}
\keyword{internal}
