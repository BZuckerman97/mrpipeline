% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{compute_ld_to_index}
\alias{compute_ld_to_index}
\title{LD (r2) of every SNP to a specified index SNP, via local reference panel}
\usage{
compute_ld_to_index(
  snps,
  index_snp,
  bfile,
  plink_bin = NULL,
  plink_threads = NULL,
  plink_memory = NULL
)
}
\arguments{
\item{snps}{Character vector of rsIDs.}

\item{index_snp}{rsID of the SNP to compute LD against. Must be one of
\code{snps}.}

\item{bfile}{Path to PLINK bfile prefix (without .bed/.bim/.fam).}

\item{plink_bin}{Path to PLINK binary. If \code{NULL}, auto-detected via
\code{\link[genetics.binaRies:get_plink_binary]{genetics.binaRies::get_plink_binary()}}.}

\item{plink_threads}{Number of threads for PLINK. \code{NULL} = auto-detect.}

\item{plink_memory}{Memory (MB) for PLINK. \code{NULL} = auto-detect.}
}
\value{
A data frame with columns \code{SNP} and \code{r2}.
}
\description{
Reuses \code{\link[=compute_ld_matrix]{compute_ld_matrix()}} -- no separate LD mechanism, no network
calls, and consistent sign/allele handling with the rest of the package.
\code{\link[=compute_ld_matrix]{compute_ld_matrix()}}'s matrix is signed (\code{--r}, not \code{--r2}) but squaring
removes the sign, which is all a colocalization regional plot's LD-colour
scale needs.
}
\keyword{internal}
