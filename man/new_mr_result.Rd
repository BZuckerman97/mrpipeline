% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_result.R
\name{new_mr_result}
\alias{new_mr_result}
\title{Create an mr_result object}
\usage{
new_mr_result(
  results = empty_mr_results(),
  instruments = data.frame(),
  harmonisation = data.frame(),
  f_stats = list(per_snp = numeric(), mean = NA_real_, min = NA_real_),
  steiger = NULL,
  pleiotropy = NULL,
  heterogeneity = NULL,
  loo = NULL,
  methods_skipped = character(),
  ld_matrix = NULL,
  params = list(),
  status = "success",
  status_reason = NULL,
  timing = numeric(0)
)
}
\arguments{
\item{results}{Data frame with columns: exposure, outcome, method, nsnp,
b, se, pval, ld_corrected (logical: whether the LD matrix was used for
that fit), model (\code{"random"}, \code{"fixed"} or \code{NA}), plus or, or_lci95,
or_uci95 (and lo_ci, up_ci) from \code{\link[TwoSampleMR:generate_odds_ratios]{TwoSampleMR::generate_odds_ratios()}}
on a successful run. Defaults to \code{\link[=empty_mr_results]{empty_mr_results()}}.}

\item{instruments}{Data frame of harmonised (and clumped) instrument data:
the kept variants the MR estimates are computed from.}

\item{harmonisation}{Data frame. The complete, unfiltered
\code{\link[TwoSampleMR:harmonise_data]{TwoSampleMR::harmonise_data()}} output -- every candidate variant, with
the \code{mr_keep}, \code{palindromic}, \code{ambiguous} and \code{remove} flags that explain
why each one was or was not carried forward. \code{instruments} is the subset
of this that survived; see \code{\link[=harmonisation_summary]{harmonisation_summary()}}.}

\item{f_stats}{List with elements \code{per_snp} (numeric vector),
\code{mean} (numeric scalar), \code{min} (numeric scalar).}

\item{steiger}{Output of \code{\link[TwoSampleMR:steiger_filtering]{TwoSampleMR::steiger_filtering()}}, or \code{NULL}.}

\item{pleiotropy}{Output of \code{\link[TwoSampleMR:mr_pleiotropy_test]{TwoSampleMR::mr_pleiotropy_test()}}, or \code{NULL}.}

\item{heterogeneity}{Output of \code{\link[TwoSampleMR:mr_heterogeneity]{TwoSampleMR::mr_heterogeneity()}} (Cochran's
Q per method), or \code{NULL}.}

\item{loo}{Output of \code{\link[TwoSampleMR:mr_leaveoneout]{TwoSampleMR::mr_leaveoneout()}}, or \code{NULL}.}

\item{methods_skipped}{Named character vector: names are method names,
values are reasons for skipping.}

\item{ld_matrix}{LD correlation matrix if \code{ld_correct = TRUE}, or \code{NULL}.}

\item{params}{List of all input parameters to \code{run_mr()}.}

\item{status}{Character. One of \code{"success"}, \code{"no_instruments"},
\code{"no_harmonised_variants"}. Default \code{"success"}.}

\item{status_reason}{Character or \code{NULL}. Human-readable explanation when
\code{status != "success"}.}

\item{timing}{Named numeric vector of elapsed times (seconds) for each
major step inside \code{run_mr()}. Empty by default.}
}
\value{
An object of class \code{mr_result}.
}
\description{
Create an mr_result object
}
\keyword{internal}
