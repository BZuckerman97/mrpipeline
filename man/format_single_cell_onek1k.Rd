% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_single_cell_eqtl.R
\name{format_single_cell_onek1k}
\alias{format_single_cell_onek1k}
\title{Format single-cell RNA eQTL data from OneK1K for MR analysis}
\usage{
format_single_cell_onek1k(onek1k_mapping, onek1k_cell_type, sample_size = NULL)
}
\arguments{
\item{onek1k_mapping}{A data frame with columns \code{cell_type} and
\code{path_to_eqtl_file}, mapping each cell type to its eQTL summary
statistics file.}

\item{onek1k_cell_type}{Character. The cell type to format (e.g.,
\code{"cd4nc"}). Must match an entry in the \code{cell_type} column of
\code{onek1k_mapping}. When the file contains a \code{CELL_ID} column, rows are
filtered to this value.}

\item{sample_size}{Integer or \code{NULL}. Sample size for this cell type,
stored as \code{samplesize.exposure}. Required by \code{\link[=run_coloc]{run_coloc()}}; if \code{NULL}
(default), no sample size column is added.}
}
\value{
A data frame formatted as a TwoSampleMR exposure dataset, including
\code{chr.exposure}, \code{pos.exposure}, and \code{eaf.exposure} columns required by
\code{\link[=run_coloc]{run_coloc()}}.
}
\description{
Reads and formats eQTL summary statistics from the OneK1K project for a
specific cell type, producing a data frame ready for \code{\link[=run_mr]{run_mr()}} or
\code{\link[=run_coloc]{run_coloc()}}.
}
\details{
OneK1K data is available from \url{https://onek1k.org/}. The expected input
format is the per-cell-type or combined eQTL table with columns:
\code{CHR}, \code{POS}, \code{RSID}, \code{A1}, \code{A2}, \code{A2_FREQ_ONEK1K},
\code{SPEARMANS_RHO}, \code{P_VALUE}, \code{GENE}, and optionally \code{CELL_ID}.

\code{A2} is the effect allele (the allele whose frequency is reported in
\code{A2_FREQ_ONEK1K}) and \code{A1} is the other allele. Spearman's rho
(\code{SPEARMANS_RHO}) is used as the effect size (beta). Standard errors are
derived from the p-value:
\verb{se = |rho| / qnorm(P_VALUE / 2, lower.tail = FALSE)}.
Rows with non-finite or zero SE are dropped. The exposure phenotype label
is \verb{GENE___<cell_type>}.

If the file contains a \code{CELL_ID} column (combined multi-cell-type file),
rows are filtered to those matching \code{onek1k_cell_type}.
}
\examples{
\dontrun{
mapping_df <- data.frame(
  cell_type         = "cd4nc",
  path_to_eqtl_file = "esnp_table.tsv.gz"
)
exposure <- format_single_cell_onek1k(
  onek1k_mapping   = mapping_df,
  onek1k_cell_type = "cd4nc",
  sample_size      = 463528L
)
}
}
\seealso{
\code{\link[=run_mr]{run_mr()}}, \code{\link[=run_coloc]{run_coloc()}}

Other sceqtl-format:
\code{\link[=format_sceqtl_1m_scbloodnl]{format_sceqtl_1m_scbloodnl()}},
\code{\link[=format_sceqtl_dice]{format_sceqtl_dice()}},
\code{\link[=format_sceqtl_dynamic_cseqtl]{format_sceqtl_dynamic_cseqtl()}}
}
\concept{sceqtl-format}
