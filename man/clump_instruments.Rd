% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{clump_instruments}
\alias{clump_instruments}
\title{Clump instruments using LD reference}
\usage{
clump_instruments(
  dat,
  rsq_thresh,
  clump_kb = 10000,
  bfile = NULL,
  plink_bin = NULL,
  pop = "EUR"
)
}
\arguments{
\item{dat}{Data frame with columns \code{rsid}, \code{pval}, \code{id}, and optionally
\code{beta} and \code{se} (used for pseudo p-value computation). If \code{beta} and
\code{se} are absent, \code{pval} is used directly.}

\item{rsq_thresh}{R-squared clumping threshold.}

\item{clump_kb}{Clumping window in kb. Default 10000.}

\item{bfile}{Path to PLINK bfile prefix. If \code{NULL}, uses API clumping.}

\item{plink_bin}{Path to PLINK binary. If \code{NULL}, auto-detected.}

\item{pop}{Population for API clumping. Default \code{"EUR"}.}
}
\value{
Data frame of clumped variants (output of \code{ieugwasr::ld_clump()}).
}
\description{
Computes pseudo p-values (to ensure correct ranking by PLINK) and
performs LD clumping via \code{\link[ieugwasr:ld_clump]{ieugwasr::ld_clump()}}.
}
\details{
Pseudo p-values are generated by:
\enumerate{
\item Computing z-scores: \code{beta / se}
\item Computing precise -log10(p) via \code{\link[Rmpfr:distr-etc]{Rmpfr::pnorm()}}
\item Ranking SNPs by -log10(p) descending
\item Assigning evenly spaced pseudo p-values from 1e-100 to 0.9
}
}
\keyword{internal}
