% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_coloc.R
\name{run_coloc}
\alias{run_coloc}
\title{Perform colocalization analysis}
\usage{
run_coloc(
  exposure,
  exposure_id,
  outcome,
  outcome_id,
  gene_chr,
  gene_start,
  gene_end,
  coloc_window = 10000L,
  exposure_n = NULL,
  outcome_n = NULL,
  exposure_type = c("quant", "cc"),
  outcome_type = c("quant", "cc"),
  exposure_s = NULL,
  outcome_s = NULL,
  exposure_sdY = 1,
  outcome_sdY = 1,
  bfile,
  plink_bin = NULL,
  methods = c("abf", "susie", "signals"),
  p1 = 1e-04,
  p2 = 1e-04,
  p12 = 1e-05,
  plink_threads = plink_option("threads"),
  plink_memory = plink_option("memory"),
  susie_maxit = 10000L,
  susie_repeat_until_convergence = FALSE,
  exclude_regions = NULL,
  ref_frq = NULL,
  verbose = TRUE
)
}
\arguments{
\item{exposure}{Data frame of formatted exposure data (output of
\code{\link[TwoSampleMR:format_data]{TwoSampleMR::format_data()}} or \verb{format_pqtl_*()} functions).}

\item{exposure_id}{Character. Label for the exposure (e.g. protein name or
trait). Used in printed output and stored in the returned object.}

\item{outcome}{Data frame of outcome summary statistics with standardised
columns: \code{rsids}, \code{chr}, \code{pos}, \code{beta}, \code{se}, \code{eaf}, \code{pval}, \code{n},
\code{effect_allele}, \code{other_allele}.}

\item{outcome_id}{Character. Label for the outcome (e.g. disease name or
trait). Used in printed output and stored in the returned object.}

\item{gene_chr}{Character or integer. Chromosome of the gene/region.}

\item{gene_start}{Integer. Start position (bp) of the gene/region.}

\item{gene_end}{Integer. End position (bp) of the gene/region.}

\item{coloc_window}{Integer. Window in base pairs to extend around the
gene region. Default \code{10000L} (10 kb).}

\item{exposure_n}{Integer. Exposure sample size. If \code{NULL}, inferred from
\code{samplesize.exposure} column.}

\item{outcome_n}{Integer. Outcome sample size. If \code{NULL}, inferred from
\code{n} column of \code{outcome}.}

\item{exposure_type}{Character. Type of exposure trait: \code{"quant"} or
\code{"cc"}. Default \code{"quant"}.}

\item{outcome_type}{Character. Type of outcome trait: \code{"quant"} or
\code{"cc"}. Default \code{"quant"}.}

\item{exposure_s}{Numeric. Proportion of cases for case-control exposure.
Required when \code{exposure_type = "cc"}.}

\item{outcome_s}{Numeric. Proportion of cases for case-control outcome.
Required when \code{outcome_type = "cc"}.}

\item{exposure_sdY}{Numeric. Standard deviation of the exposure trait
(for quantitative traits). Default \code{1}.}

\item{outcome_sdY}{Numeric. Standard deviation of the outcome trait
(for quantitative traits). Default \code{1}.}

\item{bfile}{Character. Path to PLINK bfile prefix (without
.bed/.bim/.fam) for LD reference. Required.}

\item{plink_bin}{Character. Path to PLINK binary. Auto-detected if \code{NULL}.}

\item{methods}{Character vector of colocalization methods to run.
Options: \code{"abf"}, \code{"susie"}, \code{"signals"}, \code{"prop_test"}. Default
\code{c("abf", "susie", "signals")}.}

\item{p1}{Numeric. Prior probability a SNP is associated with trait 1.
Default \code{1e-4}.}

\item{p2}{Numeric. Prior probability a SNP is associated with trait 2.
Default \code{1e-4}.}

\item{p12}{Numeric. Prior probability a SNP is associated with both
traits. Default \code{1e-5}.}

\item{plink_threads}{Integer. Number of threads for PLINK. \code{NULL} (default)
lets PLINK auto-detect. Read from \code{getOption("mrpipeline.plink_threads")}
or the \code{MRPIPELINE_PLINK_THREADS} environment variable via
\code{\link[=plink_option]{plink_option()}}.}

\item{plink_memory}{Integer. Memory limit in MB for PLINK. \code{NULL} (default)
lets PLINK auto-detect. Read from \code{getOption("mrpipeline.plink_memory")}
or the \code{MRPIPELINE_PLINK_MEMORY} environment variable via
\code{\link[=plink_option]{plink_option()}}.}

\item{susie_maxit}{Integer. Maximum iterations for \code{\link[coloc:runsusie]{coloc::runsusie()}}.
Default \code{10000L}. Increase if SuSiE warns about non-convergence; decrease
for faster exploratory runs.}

\item{susie_repeat_until_convergence}{Logical. Passed to
\code{\link[coloc:runsusie]{coloc::runsusie()}}. Default \code{FALSE} -- prevents infinite loops when
SuSiE has not converged within \code{susie_maxit} iterations.}

\item{exclude_regions}{Data frame with columns \code{chr}, \code{start}, \code{end} defining
genomic regions to exclude SNPs from before colocalization, or \code{NULL}.
SNPs in both the exposure and outcome that fall within any listed region
are dropped before harmonisation. For example, to exclude the MHC:
\code{data.frame(chr = "6", start = 26e6, end = 34e6)}.}

\item{ref_frq}{Character. Path to a PLINK \code{.frq} file (produced by
\code{plink --freq}) used to patch EAF for SNPs missing it in both the exposure
and outcome. Columns expected: \code{SNP}, \code{A1}, \code{A2}, \code{MAF}. The A1 allele is
matched against \code{effect_allele.outcome} (post-harmonisation) to orient the
frequency correctly. \code{NULL} (default) disables the lookup.}

\item{verbose}{Logical. If \code{TRUE}, emit informational messages via
\code{\link[cli:cli_inform]{cli::cli_inform()}}. Warnings and errors are always emitted regardless.
Default \code{TRUE}.}
}
\value{
A \code{coloc_result} object. Check \code{result$status} for \code{"success"} vs
failure reasons. The \verb{$timing} field contains a named numeric vector of
elapsed seconds for each major step.
}
\description{
Runs colocalization between a pre-formatted exposure and a raw outcome
dataset within a specified gene region. Supports coloc.abf, SuSiE-based
coloc (coloc.susie), coloc.signals, and colocPropTest. Returns a
\code{coloc_result} S3 object.
}
\section{Methods}{

\itemize{
\item \code{"abf"} -- Approximate Bayes Factor colocalization via
\code{\link[coloc:coloc.abf]{coloc::coloc.abf()}}
\item \code{"susie"} -- SuSiE fine-mapping + colocalization via
\code{\link[coloc:runsusie]{coloc::runsusie()}} and \code{\link[coloc:coloc.susie]{coloc::coloc.susie()}}
\item \code{"signals"} -- Multi-signal colocalization via
\code{\link[coloc:coloc.signals]{coloc::coloc.signals()}}, called with \code{method = "cond"} (LD-based
conditioning) on the same \code{dataset_exp}/\code{dataset_out} objects used by
\code{"abf"}. \code{\link[coloc:coloc.signals]{coloc::coloc.signals()}} performs its own internal signal
detection (\code{finemap.signals()}) and does not consume \code{"susie"}'s
\code{runsusie()} output -- despite the name, it's an independent multi-signal
method, not a wrapper around SuSiE's credible sets. It is still gated on
\code{"susie"} having been requested and having run successfully (skipped
otherwise), for consistency with this function's \code{methods} design rather
than because it needs SuSiE's output.
\item \code{"prop_test"} -- Proportionality test via
\code{colocPropTest::coloc.prop.test()}. Requires \code{"signals"} to have run
successfully and the \code{colocPropTest} package to be installed.
}
}

\section{MAF / EAF handling}{

\code{\link[coloc:coloc.abf]{coloc::coloc.abf()}} takes a single MAF per SNP, shared between the
exposure and outcome datasets, and errors out for the \emph{entire} dataset if
even one SNP has a missing MAF. Some GWASes (e.g. the CRP exposure used in
this pipeline) report no EAF column at all. To avoid losing every SNP over
a single dataset's missing EAF, \code{run_coloc()} builds the MAF from
\code{eaf.exposure}, falling back to \code{eaf.outcome} wherever the exposure value
is missing (and vice versa). A \code{\link[cli:cli_warn]{cli::cli_warn()}} is emitted whenever either
side has missing EAF, so the fallback is visible in the warnings captured
by \code{targets} metadata (\code{targets::tar_meta(fields = warnings)}) even though
it doesn't print live during a \code{tar_make()} run. Any SNP missing EAF on
\emph{both} sides is dropped (with a warning) before colocalization, since there
is no value left to fall back to.
}

\section{LD matrix orientation}{

\code{\link[=compute_ld_matrix]{compute_ld_matrix()}} (via \code{\link[ieugwasr:ld_matrix]{ieugwasr::ld_matrix()}}, \verb{--r square --keep-allele-order}) returns a \strong{signed} correlation matrix whose sign
is anchored to the reference panel's own, arbitrary A1 allele for each
SNP -- not to \code{effect_allele.exposure}/\code{effect_allele.outcome}. Roughly
half of SNPs, at random, will have the panel's A1 differ from the
harmonised effect allele. \code{\link[coloc:coloc.abf]{coloc::coloc.abf()}} is unaffected by this: it
never uses \code{LD}, and each SNP's Bayes factor depends only on \code{beta^2},
so a per-SNP sign flip changes nothing. \code{\link[coloc:runsusie]{coloc::runsusie()}} (and the
\code{susieR::susie_rss()} it wraps), however, fits a joint model across all
SNPs from \code{LD} and \code{beta}/\code{varbeta} together -- if even a subset of SNPs
have a sign inconsistent with \code{LD}'s convention, the fit becomes
internally incoherent, which is exactly what \code{susie_rss()}'s
\code{check_prior} safety check (comparing the fitted prior variance against
\code{100 * max(abs(z))^2}) is designed to catch, surfacing as the error
\code{"the estimated prior variance is unreasonably large"}. If you ever see
that error and \code{"abf"} succeeded on the same data, an LD-orientation bug
-- not LD-reference-panel quality -- is the first thing to suspect.
\code{\link[=align_to_ld_matrix]{align_to_ld_matrix()}} corrects for this automatically: it classifies
each SNP as matching, needing a sign flip, or unresolvable against the
reference panel (also dropping palindromic SNPs with an EAF in the
ambiguous 0.42-0.58 zone, which can't be resolved by allele identity
alone), and returns a \code{ld_sign} vector multiplied into
\code{dataset_exp$beta}/\code{dataset_out$beta} immediately before they're passed
to \code{coloc::runsusie()} -- see that function's documentation for the full
algorithm.
}

\examples{
\dontrun{
result <- run_coloc(
  exposure = formatted_exposure,
  outcome = outcome_data,
  gene_chr = 20, gene_start = 44746911, gene_end = 44758502,
  bfile = "/path/to/ld_reference",
  methods = "abf"
)
print(result)
summary(result)
}
}
\seealso{
\code{\link[=new_coloc_result]{new_coloc_result()}} for the S3 class structure,
\code{\link[=print.coloc_result]{print.coloc_result()}} and \code{\link[=summary.coloc_result]{summary.coloc_result()}} for display methods.
}
\concept{colocalization}
