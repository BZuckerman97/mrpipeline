# Validate exclude_regions argument

Validate exclude_regions argument

## Usage

``` r
validate_exclude_regions(exclude_regions)
```

## Arguments

- exclude_regions:

  Data frame to validate.

## Value

Invisibly returns `TRUE` if valid; otherwise aborts with an error.
